describe('Check for all required route to call the AddMetaFilter', function () {
  it('check for filter in config with route', function () {})
  it('check for filter in config with value + route', function () {})
})
describe('Check for all required route not to call the AddMetaFilter', function () {
  it('framework filter on route call, if getConfig returns null, then call next', function () {})
  it('framework filter on route call, if request body has the property framework, then call next', function () {})
})
